/**
 * ParentHome component – re-exports the parent/dashboard screen content
 * as a component so the role-aware index.tsx can render it directly.
 */
export { default } from '@/app/parent/dashboard';
